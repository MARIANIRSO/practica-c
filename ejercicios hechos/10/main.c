//10- Escribir en C un programa que muestre los números impares entre 0 y 100
// y que imprima cuantos impares hay
#include <stdio.h>
#include <stdlib.h>

int main()
{
   int i;
 for(i=0;i<100;i++)
 {
    if(i%2==0);

    else
      printf("\n los numeros impares son: %d \n", i );
 }
    return 0;
}
